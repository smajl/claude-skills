---
name: example-skill
description: Placeholder skill — replace this description with a specific trigger ("use when...") once you write the real skill.
---

# Example skill

Replace this with the actual skill instructions. Keep the frontmatter
`description` specific about *when* Claude should invoke this skill —
that's the only thing Claude sees before deciding to load it.
